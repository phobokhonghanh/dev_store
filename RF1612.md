**1. yarn, npm, pnpm là gì? Hoạt động như thế nào? Depcheck có thật sự cần thiết cho yarn và npm?**
_npx depcheck_

**2. Analytics (GA_MEASUREMENT_ID, POSTHOG_API_KEY, SELINE_TOKEN)**

**3. turbo là gì? Cách sử dụng?**

**4. Husky là gì? Cách sử dụng?**

**5. Commitlint là gì? Cách sử dụng và kết nối với Husky v9 ?**

**6. Commitizen? Làm sao để sử dụng?**

**7. Khắc phục lỗi Lint do xung đột phiên bản**

**Vấn đề:**
Ban đầu, lệnh `yarn lint` thất bại với lỗi không tìm thấy lệnh `next lint`. Sau khi điều chỉnh để sử dụng `eslint .` trực tiếp, xuất hiện lỗi `TypeError: Converting circular structure to JSON` trong quá trình xác thực cấu hình ESLint. Lỗi này bắt nguồn từ sự không tương thích giữa phiên bản Next.js 16.x (một phiên bản thử nghiệm) và tiện ích `FlatCompat` của ESLint khi cố gắng mở rộng cấu hình `eslint-config-next`. Vấn đề này ảnh hưởng đến cả các ứng dụng Next.js (như `home`, `blog`) và các gói thư viện khác (như `@origini/interfaces`, `@origini/config`).

**Giải pháp:**
Để giải quyết vấn đề tương thích phiên bản mà không cần hạ cấp Next.js, chúng tôi đã thực hiện các thay đổi sau đối với cấu hình ESLint tùy chỉnh:

1.  **Đối với các ứng dụng Next.js (`apps/home`, `apps/blog`):**
    - Sửa đổi `packages/eslint-config-custom/next.mjs`.
    - Thay vì mở rộng `eslint-config-next` thông qua `FlatCompat`, chúng tôi đã xây dựng một cấu hình ESLint tùy chỉnh thủ công, bao gồm các quy tắc TypeScript và React được đề xuất.
    - Vô hiệu hóa quy tắc `@typescript-eslint/no-require-imports` cho các tệp `.js` và `.cjs` (ví dụ: `tailwind.config.js`) để tránh lỗi lint không liên quan đến vấn đề chính.

2.  **Đối với các gói thư viện (`@origini/interfaces`, `@origini/config`, v.v.):**
    - Sửa đổi `packages/eslint-config-custom/index.mjs`.
    - Các gói này trước đây đã cố gắng mở rộng `eslint-config-next` một cách không chính xác.
    - Chúng tôi đã thay thế bằng một cấu hình ESLint tùy chỉnh tối giản, chỉ tập trung vào các quy tắc TypeScript, vì các gói này không phải là ứng dụng Next.js hoặc React.

Những thay đổi này đã khắc phục tất cả các lỗi và cho phép lệnh `yarn lint` chạy thành công.
